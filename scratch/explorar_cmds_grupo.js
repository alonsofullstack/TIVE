/**
 * explorar_cmds_grupo.js
 * 
 * NOTA: Este script no puede correr independiente porque compite con el bot principal
 * por la sesión de Telegram. 
 * 
 * En su lugar, usa el comando /explorar_cmds dentro del bot (solo admin).
 * El bot usará su propio userbot ya conectado para hacer el recorrido.
 */
console.log('⚠️  Usa /explorar_cmds dentro del bot (solo admin) en lugar de este script.');
console.log('   El bot ya tiene el userbot conectado y puede hacer el recorrido sin conflictos.');
